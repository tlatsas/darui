* add support for YAML configuration files (low priority)
* hold (somewhere) a .state file with feed dates to
  avoid duplicate results in emails
* maybe use a url shortener for links send in emails
* add better error handling at email_results()
